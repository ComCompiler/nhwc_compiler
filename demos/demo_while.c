/* Type your code here, or load an example. */
int square(int num) {
    return num*num;
}
int main(){
    int x = 4;
    while (x < 17){
        x = x+1;
    }
    int b = square(5);
    breakpoint exit;
    return 0;
}
